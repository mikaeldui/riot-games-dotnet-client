name: .NET Release
on: 
  workflow_call:
    inputs:
      version:
        description: 'The version being released'
        required: true
        type: string        
    secrets:
      NUGET_ORG_API_KEY:
        required: true
  
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.0.x

      - name: Download NuGet Package
        uses: actions/download-artifact@v2
        with:
          name: nupkg

      - name: Push
        uses: ./.github/actions/dotnet-push
        with:
          GITHUB_TOKEN: ${{ github.token }}
          NUGET_ORG_API_KEY: ${{ secrets.NUGET_ORG_API_KEY }}

      - name: Create tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          custom_tag: ${{ inputs.version }}

      - uses: ncipollo/release-action@v1
        with:
          artifacts: "*.nupkg"
          token: ${{ github.token }}
          name: ${{ inputs.version }}
          tag: ${{ steps.tag_version.outputs.new_tag }}
          body: |
            You can install this specific version using either of these commands:

            Using Package Manager Console:

                PM> Install-Package MikaelDui.RiotGames.Client --Version ${{ inputs.version }}

            Using the .NET CLI:

                dotnet add package MikaelDui.RiotGames.Client --version ${{ inputs.version }}

            But it's preferable to use **floating versions**, see [README.md](https://github.com/mikaeldui/riot-games-dotnet-client/blob/main/README.md).

            ${{ steps.tag_version.outputs.changelog }}
